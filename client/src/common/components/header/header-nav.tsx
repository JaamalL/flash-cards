import s from "@/common/components/header/styles/style.module.scss";

export const HeaderNav = () => (
    <nav className={s.header_wrapper_nav}>
        <ul className={s.header_wrapper_nav_list} />
    </nav>
);